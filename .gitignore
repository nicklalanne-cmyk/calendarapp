/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Regenerated by scripts/copy-pdf-worker.js on every `npm install` — always
# matches whatever pdfjs-dist version is installed, so it's not committed.
/public/pdf.worker.min.mjs
